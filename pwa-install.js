const params=new URLSearchParams(location.search);const parts=location.pathname.split('/').filter(Boolean);const slug=params.get('slug')||(parts[0]==='install'&&parts[1])||'demo';const token=params.get('token')||'';const company=params.get('bedrijf')||slug.replace(/-/g,' ').replace(/\b\w/g,c=>c.toUpperCase());const appUrl=params.get('app')||location.origin;const title=document.getElementById('title'),button=document.getElementById('installButton'),status=document.getElementById('status'),iosHelp=document.getElementById('iosHelp'),manifest=document.getElementById('manifestLink');manifest.href=`/manifest/${encodeURIComponent(slug)}.webmanifest?token=${encodeURIComponent(token)}`;title.textContent=`BouwFlow voor ${company}`;localStorage.setItem('bouwflow_customer',JSON.stringify({slug,company,token}));if('serviceWorker'in navigator)navigator.serviceWorker.register('/sw.js').catch(()=>{});const standalone=matchMedia('(display-mode: standalone)').matches||navigator.standalone===true;const isiOS=/iphone|ipad|ipod/i.test(navigator.userAgent);let deferredPrompt=null;const appLink=()=>`/app/${encodeURIComponent(slug)}?token=${encodeURIComponent(token)}`;if(standalone){button.textContent='BouwFlow openen';status.textContent='BouwFlow is al geinstalleerd op dit apparaat.';button.onclick=()=>location.href=appLink()}else if(isiOS){button.textContent='Bekijk installatie-uitleg';iosHelp.style.display='block';status.textContent='Op iPhone en iPad gebruikt Apple Zet op beginscherm.';button.onclick=()=>iosHelp.scrollIntoView({behavior:'smooth'})}else{button.disabled=true;button.textContent='Installatie voorbereiden...';status.textContent='Een moment, BouwFlow wordt klaargezet.'}addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferredPrompt=e;button.disabled=false;button.textContent='Installeer BouwFlow';status.textContent='Klaar om te installeren.'});button.addEventListener('click',async()=>{if(!deferredPrompt)return;deferredPrompt.prompt();const r=await deferredPrompt.userChoice;status.textContent=r.outcome==='accepted'?'BouwFlow wordt geinstalleerd...':'Installatie geannuleerd. Je kunt het opnieuw proberen.';deferredPrompt=null});addEventListener('appinstalled',()=>{button.disabled=false;button.textContent='BouwFlow is geinstalleerd ✓';status.textContent='Klaar. De demo blijft 7 dagen actief.'});setTimeout(()=>{if(!standalone&&!isiOS&&!deferredPrompt){button.disabled=false;button.textContent='Open BouwFlow';status.textContent='Je kunt BouwFlow direct openen.';button.onclick=()=>location.href=appLink()}},3000);